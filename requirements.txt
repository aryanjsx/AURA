# Phase 1 — Core runtime
psutil>=5.9.0
pyyaml>=6.0
httpx>=0.27

# Development / testing
pytest>=7.0

# Phase 2 — Voice Pipeline
openai-whisper>=20231117
sounddevice>=0.4.6
# Wake Word — Primary Engine (models auto-download on first run)
openwakeword>=0.6.0
onnxruntime>=1.10.0
# # PyAudio>=0.2.13          # Optional — needed for Porcupine wake word only (requires C++ Build Tools)  # requires Visual C++ Build Tools on Windows; sounddevice is used instead
pvporcupine>=3.0.0
piper-tts>=1.2.0
edge-tts>=6.1.9
pyttsx3>=2.90
keyboard>=0.13.5
soundfile>=0.12.1

# Phase 2 — LLM Brain
langchain>=0.2.0
langchain-community>=0.2.0

# Phase 2 — Utilities
numpy>=1.24.0
python-dotenv>=1.0.0
pyautogui>=0.9.54
pycaw>=20230407      # Windows volume control — optional, graceful fallback

# Phase 3 — Dev Tools (uncomment when ready)
# gitpython             # Git automation
# docker                # Docker SDK

# Phase 5 — GUI (uncomment when ready)
# PyQt6                 # Desktop dashboard

# Phase 6 — Memory (uncomment when ready)
# chromadb              # Vector memory

# Phase 7 — Browser (uncomment when ready)
# playwright            # Web automation
